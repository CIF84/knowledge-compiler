# SPEC-020 realistic semantic depth

This directory freezes the trusted SPEC-013 parent, the `double-slit-experiment` focus, the exact Wave–particle duality source section, the seven reused parent symbols, and the two-call assertion-first configuration before any live output.

Live (only after explicit approval):

```sh
.venv/bin/knowledge-compiler evaluate-semantic-depth --output-dir examples/evaluations/spec-020-realistic-semantic-depth-20260905 --model gpt-5.6-luna
```

Review after trusted finalization:

```sh
.venv/bin/knowledge-compiler view-representations examples/evaluations/spec-020-realistic-semantic-depth-20260905 --port 8021
```
