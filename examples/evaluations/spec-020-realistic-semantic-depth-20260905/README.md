# SPEC-020 realistic semantic depth

This directory preserves the complete SPEC-020 experiment over the trusted SPEC-013 parent, the preselected `double-slit-experiment` focus, the exact Wave–particle duality source section, and seven reused parent symbols.

The approved live command was:

```sh
.venv/bin/knowledge-compiler evaluate-semantic-depth --output-dir examples/evaluations/spec-020-realistic-semantic-depth-20260905 --model gpt-5.6-luna
```

Call 1 produced nine exactly grounded assertions. Conditional Call 2 produced three relationships and six claims, then failed closed because no detected child representation retained the frozen focus. The rejected proposal was not rendered and no owner cognitive review was opened. The final verdict is `INCONCLUSIVE`.

No viewer command is available because SPEC-020 forbids rendering a rejected child as trusted learning content. See `failure-assessment.json`, `rejected-semantic-review.json`, and `run-history.json` for the preserved result.
