# SPEC-033 canonical revealed-knowledge tree

This isolated offline candidate preserves the trusted semantic engine while making the left surface a canonical, deduplicated, collapsible view of what has been revealed.

```sh
.venv/bin/knowledge-compiler view-representations examples/evaluations/spec-033-canonical-revealed-knowledge-tree-20260907 --port 8033
```

Use `?fixture=large` for the deterministic 60-object scaling fixture.
