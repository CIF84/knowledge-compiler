# SPEC-009 strategy review

This directory preserves all three Generic/type-aware pairs. Explore actions exist only for successful, validated children. Capture spontaneous reaction before completing the human-review template.

```sh
.venv/bin/knowledge-compiler view-representations examples/evaluations/spec-009-resolution-strategy-20260903 --port 8000
```

Read `repository-semantic-review.json` before owner review. Deterministic grounding and
canonical-direction validation passed for both generated children, but independent semantic
inspection found endpoint-modeling defects that the current validator does not detect. The
owner-facing template remains intentionally unanswered.

`spec-008-positive-control-comparison.json` compares the new Economics Generic run with the
accepted SPEC-008 Generic child without using lexical overlap as a quality score.
