# SPEC-023 — Realistic Semantic Depth Through Explanatory Projection

Status: `IMPLEMENTED_AWAITING_REVIEW`
Authority: `OFFLINE_ONLY`
Human gate: `OWNER_REVIEW`
Promotion: `NOT_AUTHORIZED`

## Primary uncertainty

> Does the already-generated, source-grounded double-slit explanatory projection provide useful semantic depth when experienced through the accepted BASELINE-004 learner-navigation workspace?

## Why this is next

SPEC-020 attempted the product question but never reached owner review because ordinary canonical structure admission omitted the requested focus.

SPEC-021 then created a focus-preserving explanatory projection from the exact frozen SPEC-020 grounded semantic packet, but its first viewer invalidated the experiment by regressing the workspace shell. The shell was subsequently restored and SPEC-022 resolved the learner interaction grammar. SPEC-022 was owner-approved as `NAVIGATION_GRAMMAR_BETTER` and promoted unchanged to BASELINE-004.

We now have all required ingredients without another semantic/model experiment:

- the frozen realistic quantum source and SPEC-020 semantic packet;
- the deterministic SPEC-021 explanatory projection for `double-slit-experiment`;
- BASELINE-004 as the accepted executable learner workspace;
- contextual depth behavior established by SPEC-022.

The remaining question is finally the original human learning question: **is this deeper representation actually useful to think with?**

## Frozen control

BASELINE-004 is the executable UI control.

Do not recreate, redesign, polish, or reinterpret it.

Preserve byte-for-byte all frozen BASELINE-001 through BASELINE-004 assets.

Use the actual executable BASELINE-004 implementation/assets as the workspace shell and interaction grammar.

## Frozen semantic inputs

Reuse only existing committed artifacts from SPEC-020 / SPEC-021.

No semantic regeneration is permitted.

Focus remains:

```text
double-slit-experiment
```

Use the exact preserved grounded assertions, symbol inventory, source scope/evidence, canonicalization result/review, and explanatory projection already produced by those experiments.

The known overstated causal item remains rejected/demoted exactly as previously reviewed.

Do not repair, reinterpret, or add quantum facts.

## Experimental variable

The only experimental variable is **human experience of the existing explanatory depth inside BASELINE-004**.

The implementation task is therefore integration/fixture preparation, not semantic invention and not UI design.

## Required learner path

The fixed review artifact must permit the owner to reach the deeper double-slit representation through the normal BASELINE-004 learner grammar:

```text
WORLD
  ↓
select / arrive at double-slit-experiment
  ↓
UNDERSTAND
  ↓
contextual Explore deeper affordance
  ↓
DEEPEN
  ↓
focus-preserving explanatory projection
  ↓
select explanatory/canonical items
  ↓
inspect detail/evidence
  ↓
RETURN
  ↓
parent learning state restored
```

Do not add a global `Double-slit depth` control.

Do not expose compiler terms such as `GENERIC_DETAIL`, `PRIMARY`, `SPARSE`, assertion-first, or projection internals in learner mode.

## BASELINE-004 interaction invariants

Preserve:

- two-pane `WHERE AM I?` / `WHAT DOES THIS MEAN?` workspace;
- continuous left knowledge world;
- stable world coordinates;
- pan / geometric zoom / Overview;
- region entry and orientation;
- automatic learner-facing representation selection;
- concept and relationship selection;
- map ↔ learning synchronization;
- focus suppression;
- contextual depth eligibility;
- parent camera independence from semantic depth;
- clear depth return behavior;
- detail/evidence interaction;
- current styling/layout/interaction behavior.

The experiment fails mechanically if these are substituted or materially regressed.

## Explanatory projection invariants

Preserve the SPEC-021 semantic-tier distinction.

### Canonical material

Only already trusted canonical semantic items may appear as canonical relationships.

### Source-backed explanatory material

Assertion-derived explanatory items remain explicitly non-canonical and participant-preserving.

They must not be converted into pairwise canonical edges.

Full assertion/evidence prose stays hidden until selected/requested.

The learner must be able to distinguish strong canonical structure from source-backed explanation without needing compiler terminology.

## No new representation algorithm

Do not alter the explanatory projection construction algorithm merely to make the artifact look better.

If the frozen projection cannot be integrated cleanly into BASELINE-004 without changing its semantic or representation behavior, stop and report the incompatibility.

## No live calls

This experiment is `OFFLINE_ONLY`.

Forbidden:

- OpenAI/provider calls;
- web retrieval;
- external enrichment;
- embeddings;
- semantic retry;
- assertion extraction;
- canonicalization;
- new model/agent judge.

## Mechanical preparation

Codex should perform only the minimum work needed to produce a deterministic owner-review artifact using the existing frozen inputs and BASELINE-004 shell.

Prefer composition/reuse over copied reimplementation.

Add regression checks where needed to guarantee:

- BASELINE-004 executable asset identity;
- frozen semantic packet identity;
- projection identity/semantics;
- contextual-depth eligibility only on the appropriate focus;
- parent camera preservation;
- return-state restoration;
- deeper-item interaction and evidence synchronization.

## Machine gate

Before owner review verify at least:

```text
BASELINE-001..004 hashes unchanged
BASELINE-004 executable control reused
SPEC-020 frozen semantic input hashes unchanged
SPEC-021 projection semantic payload unchanged
focus = double-slit-experiment
known rejected causal item not promoted
pairwise-edge fabrication = 0
contextual depth only where admitted
left-map camera unchanged by depth transition
return restores parent learning state
deeper canonical/explanatory items selectable
evidence/provenance resolvable
browser console clean in normal interaction
full offline suite passes
```

If any machine gate fails, do not ask for owner cognitive review.

## Owner review handoff

If the machine gate passes, Codex must use the low-attention handoff protocol.

Do **not** send the full machine inventory first.

Provide only completion state, test summary, repository synchronization state, owner-decision requirement, durable report path, and the exact viewer command.

The human-review instruction is:

```text
Use the map naturally. Find the double-slit experiment, explore it more deeply when the interface offers that option, and tell me whether the deeper view actually helps you understand it.
```

Do not explain the explanatory-projection architecture before owner interaction.

## Owner review questions

After spontaneous feedback, ChatGPT may evaluate:

### Understanding

- Does the deeper view create a useful mental model of the double-slit experiment?
- Does it add understanding beyond the parent representation?
- Are meaningful participants/observations visible without reading a document?

### Cognitive load

- Does the deeper view remain spatial/visual rather than assertion-card heavy?
- Is detail available on demand rather than dominating orientation?

### Trust

- Can canonical relationships be distinguished from weaker source-backed explanatory material?
- Does anything look stronger/more certain than the source warrants?
- Is evidence easy to inspect when desired?

### Orientation

- Does the parent knowledge world remain a stable anchor?
- Is the depth transition contextual and understandable?
- Is return obvious and non-destructive?

## Verdict set

Final verdict after owner review must be exactly one of:

```text
SEMANTIC_DEPTH_BETTER
MIXED
NO_MEANINGFUL_IMPROVEMENT
INCONCLUSIVE
```

Codex must not assign the subjective verdict.

## Success criterion

`SEMANTIC_DEPTH_BETTER` requires owner evidence that the frozen deeper representation adds useful understanding while BASELINE-004 orientation/interaction remains intact and semantic-strength distinctions remain trustworthy.

## Explicit non-goals

Do not implement:

- another UI shell;
- another baseline;
- new region/navigation grammar;
- new semantic depth generation;
- recursive depth;
- semantic zoom gestures;
- personalization;
- guided course/Journey mode;
- new representation archetypes;
- new semantic predicates/propositions;
- production ingestion/productization;
- simulation.

## Evidence

Create/update an isolated SPEC-023 evaluation directory under:

```text
examples/evaluations/spec-023-realistic-semantic-depth-<date>/
```

Store detailed machine evidence there. Keep the visible handoff concise.

## Acceptance criteria

- [ ] Exact BASELINE-004 executable shell reused.
- [ ] BASELINE-001 through BASELINE-004 preserved byte-for-byte.
- [ ] Exact frozen SPEC-020 semantic packet reused.
- [ ] Exact SPEC-021 explanatory semantic projection reused without semantic redesign.
- [ ] No live/model/external calls.
- [ ] `double-slit-experiment` remains focus.
- [ ] Known overstated causal item remains excluded/demoted.
- [ ] No fabricated canonical pairwise edges.
- [ ] Depth affordance is contextual, not global.
- [ ] Deeper view is interactive.
- [ ] Canonical vs source-backed explanatory status remains distinguishable.
- [ ] Full prose/evidence hidden until requested.
- [ ] Left-map camera remains stable across depth transition.
- [ ] Return restores parent learning state.
- [ ] BASELINE-004 pan/zoom/region/concept/relationship behavior remains intact.
- [ ] Browser console clean.
- [ ] Full offline suite passes.
- [ ] Codex provides low-attention owner-review handoff only after machine gate passes.
- [ ] Final subjective verdict remains pending owner review.

## Required durable machine report

Record at least:

- exact baseline hashes;
- frozen input/projection hashes;
- files changed;
- integration seam;
- machine gate results;
- contextual-depth eligibility result;
- camera/return-state result;
- deeper interaction/evidence result;
- deterministic regeneration result;
- offline test result;
- dependencies/live calls;
- deviations;
- commit/push state;
- viewer command.

## Final constraint

Do not improve the semantic packet and do not improve the UI.

This experiment exists to answer one question with the strongest already-earned artifacts:

> **When trustworthy deeper meaning is placed inside the accepted learner workspace, does it actually help the learner understand?**
