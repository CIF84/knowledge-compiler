# SPEC-017 ADDENDUM — Required Handoff

## Contract Status

This file is part of the canonical SPEC-017 contract together with:

```text
specs/SPEC-017-cognitive-topology-projection.md
```

Codex must read both files before implementation.

## Required Handoff

Report:

1. cognitive-projection architecture implemented;
2. exact frozen SPEC-013 inputs;
3. input identity/hash verification;
4. confirmation SPEC-016 remained unchanged;
5. presentation-affinity formula/signals;
6. exact distinction between presentation affinity and semantic relationships;
7. neighborhood derivation strategy;
8. confirmation no semantic neighborhood labels were invented;
9. deterministic salience strategy;
10. initial visible concept-selection strategy;
11. initial visible concept count;
12. total discoverable concept count;
13. layout strategy;
14. deterministic-layout controls;
15. canonical relationship visual grammar;
16. presentation-topology visual grammar;
17. concept-focus interaction;
18. local-neighborhood behavior;
19. global-orientation preservation behavior;
20. explanation reveal behavior;
21. evidence reveal behavior;
22. search/recenter behavior if implemented;
23. initial visible prose word count;
24. initial visible paragraph/card count;
25. SPEC-016 initial prose/card comparison;
26. assertion/claim availability despite default hiding;
27. provenance completeness;
28. semantic-edge fabrication checks;
29. presentation metadata labeling checks;
30. machine diagnostics;
31. layout overlaps/crossings;
32. neighborhood counts/sizes;
33. deterministic regeneration result;
34. exact offline test result;
35. dependencies added/removed;
36. complexity impact;
37. known visual/cognitive weaknesses;
38. deviations;
39. exact generation command;
40. exact viewer command;
41. local URL/port example;
42. machine integrity verdict;
43. human review status, initially `PENDING_OWNER_REVIEW` unless already completed;
44. implementation commit SHA;
45. evaluation/final commit SHA if separate;
46. push confirmation to canonical `origin/main`;
47. confirmation `HEAD == origin/main`;
48. working-tree state;
49. confirmation no `DEBRIEF-017` was created.

## Human Review Handoff Rule

Before owner cognitive review, do **not** provide the full handoff above.

First provide only:

```text
<viewer command>

Use this to orient yourself in the quantum-mechanics material.
```

Wait for spontaneous owner feedback.

Only after that feedback should implementation metrics and rationale be discussed.

## Decision Gate After SPEC-017

### If `TOPOLOGY_FIRST_BETTER`

The cognitive-compiler hypothesis has meaningful support. Decide whether to:

- refine navigation across the same topology;
- resume bounded semantic zoom;
- or validate the representation on a second realistic source/domain.

Prefer whichever reduces the largest remaining uncertainty.

### If `MIXED`

Localize the cognitive problem before changing semantic architecture.

### If `NO_MEANINGFUL_IMPROVEMENT`

Do not return to text-heavy assertion surfaces and do not loosen semantics. Reconsider the topology projection itself.

### If `INCONCLUSIVE`

Resolve only the implementation/review blocker.

## Preserved Product Principle

> **Topology first. Text on demand.**

The cognitive compiler controls attention, not truth.
